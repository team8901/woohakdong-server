spring:
  application:
    name: woohakdong-server-test

  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  cloud:
    aws:
      s3:
        bucket-name: test-bucket

jwt:
  secret: testsecrettestsecrettestsecret1234

cookie:
  secure: false
  same-site: Lax
  domain: .woohakdong.com
